# Express-Typescript

This is an implementation for e2ee built with [express](https://expressjs.com/) and [typescript](https://typescriptlang.org/).

## Usage

Start the server with `npm start` or `yarn start`.
Start the server with reloading with `npm run start:dev` or `yarn start:dev`.
